# AUR files

The canonical AUR files are kept at the repository root:

- `PKGBUILD`
- `.SRCINFO`
- `pacmangr`
- `LICENSE`

This directory is intentionally only a pointer so the root stays directly
usable as an AUR working tree with `makepkg`.
